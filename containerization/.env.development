# =============================================================================
# ASOOS MCP Development Environment Variables
# USPTO Customer: 208576
# =============================================================================

# Core Configuration
NODE_ENV=development
CUSTOMER_NUMBER=208576
LOG_LEVEL=debug

# Network Configuration  
INTEGRATION_GATEWAY_PORT=8080
MCP_VISION_LAKE_PORT=8081
MCP_ENHANCED_CIVILIZATION_PORT=8082

# Service URLs (Internal Docker Network)
MCP_VISION_LAKE_URL=http://mcp-vision-lake:8080
MCP_ENHANCED_CIVILIZATION_URL=http://mcp-enhanced-civilization:8080
TENANT_REGISTRY_URL=http://integration-gateway:8080/registry

# Authentication (Development Only - DO NOT USE IN PRODUCTION)
SALLYPORT_JWT_SECRET=dev-secret-key-change-in-production-immediately
CLOUDFLARE_API_TOKEN=dev-token-replace-with-real-token
VICTORY36_ENABLED=false

# OAuth Configuration (Development)
ANTHROPIC_INTEGRATION=enabled
OAUTH_ENABLED=true
OAUTH_CLIENT_ID=dev-client-id
OAUTH_CLIENT_SECRET=dev-client-secret
OAUTH_REDIRECT_URI=http://localhost:8080/oauth/callback

# MCP Protocol Configuration
MCP_PROTOCOL_VERSION=1.0
MCP_DISCOVERY_ENABLED=true
MCP_TOOLS_ENABLED=true

# CORS Configuration for Development
CORS_ORIGINS=https://claude.ai,https://chatgpt.com,https://console.anthropic.com,http://localhost:3000,http://localhost:8080,http://localhost:8081,http://localhost:8082

# Agent Configuration
VISION_LAKE_AGENT_COUNT=505000
VISION_LAKE_WING_COUNT=3
VISION_LAKE_SQUADRON_COUNT=6
VISION_LAKE_DIDC_BLOCKS=847293

ENHANCED_CIVILIZATION_AGENT_COUNT=20000000
ENHANCED_CIVILIZATION_WING1_LEADERS=5000
ENHANCED_CIVILIZATION_ORIGIN_PILOTS=11

# Development Database (if needed)
# DATABASE_URL=postgresql://dev_user:dev_password@localhost:5432/asoos_dev

# External Service URLs (Development)
# OPENAI_API_KEY=dev-openai-key
# PAYPAL_API_KEY=dev-paypal-key  
# SHOPIFY_ACCESS_TOKEN=dev-shopify-token

# Monitoring and Logging
ENABLE_METRICS=true
ENABLE_HEALTH_CHECKS=true
HEALTH_CHECK_INTERVAL=30s

# Development Flags
HOT_RELOAD=true
DEBUG_MODE=true
VERBOSE_LOGGING=true

# =============================================================================
# SECURITY WARNING:
# This file contains development credentials that are NOT secure.
# Never commit real credentials to version control.
# Always use proper secret management in production.
# =============================================================================
